// we can create a struct to generate each type of move since pieces share similar moves

pub struct Rays {
    // each ray holds a vector of bit boards
    n_rays: Vec<usize>,

}

impl Rays {
    fn initialize() -> Self {
        let n_rays = vec!();
        
    }
}